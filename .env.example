# Copy to .env only if these ports conflict with software on your machine.
POSTGRES_PORT=5432
ADMINER_PORT=8080
# Optional image overrides, useful for an internal registry or offline cache.
# POSTGRES_IMAGE=postgres:16-alpine
# ADMINER_IMAGE=adminer:4-standalone

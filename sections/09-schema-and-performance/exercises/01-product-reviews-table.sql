-- Create shop.product_reviews with:
--   review_id: generated identity primary key
--   product_id: required FK to products(product_id), ON DELETE CASCADE
--   rating: required integer constrained from 1 through 5
--   body: optional text
--   created_at: required timestamptz defaulting to current_timestamp

-- TODO: write CREATE TABLE.


-- Create shop.completed_order_totals as a view with one row per completed order.
-- Columns: order_id, customer_id, order_date, total_amount
-- total_amount is discounted line revenue rounded to 2 decimals.
-- Do not put ORDER BY in the view.

-- TODO: write CREATE VIEW.


-- Find customers that have never placed an order.
-- Columns: customer_id, company_name
-- Use NOT EXISTS. Sort by customer_id.

-- TODO
SELECT customer_id, company_name
FROM customers
WHERE false;


-- Return the five most expensive active products.
-- Columns, in order: sku, product_name, unit_price
-- Sort by unit_price descending, then sku ascending.

-- TODO: replace this starter query.
SELECT sku, product_name, unit_price
FROM products
WHERE false;

